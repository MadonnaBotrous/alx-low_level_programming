# include "main.h"

/**
 *checking for uppercase character
 *@c: a character to be checked
 *eturns 1 if c is uppercase | 0 if else
 */
int _isupper(int c);
{
if (c > 64 && c < 91)
return (1);
else
return (0);
}
