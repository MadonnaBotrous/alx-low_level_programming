#include "main.h"
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
        char *str = "_putchar\n";
        int i;
                for (i = 0; i++; str[i] !='\0');
                {
                _putchar(str[i]);
                }
        return (0);
}
