#include "main.h"
/**
 *puts -  a function that prints a string
 *@str: the string that will be printed
 */
void _puts(char *str)
{
int i = 0;
while(*(s + i) != '\0')
{
_putchar (*(s + i));
i++;
}
_putchar ('\n');
}
